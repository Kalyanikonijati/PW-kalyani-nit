# add job should trigger every hour
# i should only tests on kalyani.specs
# use ubuntu 

name: Run kalyani.specs

on:
  workflow_dispatch: 
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  schedule:
      - cron: '* * 1 * *'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      name: Check out repository code

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '20'

    - name: Install packages
      run: npm install

    - name: Install Playwright browsers
      run: npx playwright install
   
<<<<<<< HEAD
    - name: Running Specific page
      run: npx playwright test ./tests/kalyani.spec.ts 

=======
    - name: run tests
      run: npm practrice workflow
>>>>>>> 8e8d9dd064a5b5fdad01140bf6d79d935d372365
